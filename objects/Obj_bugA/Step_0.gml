/// @description Insert description here
// You can write your code in this editor

if (!place_meeting(x, y, Obj_DirtTile) && !instance_exists(attackAnimID))
{
	// attackingObj.tileHP -= 1000;
    // path_speed = 5
}