/// @description Exits from the mini menu

// Makes tiles interactable once this button is pressed
global.tilesInteractable = true;

// Destroys the two buttons pertaining to the tile
instance_destroy(Obj_gIcon1);
instance_destroy();